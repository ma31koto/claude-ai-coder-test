name: AI Auto Coding (Claude)

on:
  issues:
    types: [labeled]

jobs:
  auto-code:
    if: github.event.label.name == 'ai-code'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install langchain-anthropic langchain-core

      - name: Run AI Coder
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_EVENT_PATH: ${{ env.GITHUB_EVENT_PATH }}
        run: python ai_coder.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "feat: Claude-generated code for Issue #${{ github.event.issue.number }}"
          title: "Claude-Generated: ${{ github.event.issue.title }}"
          body: "Fixes #${{ github.event.issue.number }} via Claude 3.5 Haiku."
          branch: "claude-fix-issue-${{ github.event.issue.number }}"
